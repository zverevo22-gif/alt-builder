# =====================================================================
#  Накладка на метапрофиль mkimage-profiles (overlay)
#
#  Содержимое этого каталога scripts/gen-profile.sh переносит в рабочую
#  копию метапрофиля (work/mkimage-profiles), после чего конфигурация
#  из config/*.conf переписывается в conf.d/999-alt-desktop.mk.
#
#  conf.d/*.mk включаются основным Makefile метапрофиля (main.mk).
#  Здесь должны находиться только ВАШИ файлы; файлы с теми же именами,
#  что и у штатных, перезапишут штатные.
# =====================================================================

# (каталог заполняется сценарием сборки)